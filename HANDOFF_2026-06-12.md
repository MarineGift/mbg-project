# URM 세션 Handoff — 2026-06-12 (최종)

Repo: `MarineGift/mbg-project` · branch `marinebiogroup` · local `C:\dev\mbg-project`
Live: `urm.marinebiogroup.com` (Railway, push = auto-deploy)
Supabase: project `ogenmrgxwhpbfepeldqx`, org_id `b25de8f2-1020-482f-9012-183f63883169`, schema `app`
워크플로: 파일 → Downloads → ASCII mover .ps1 → `npx tsc --noEmit`(0) → git add/commit/push.
**중요: SQL은 반드시 Supabase SQL Editor에서 별도 Run 해야 DB에 반영됨.** git commit/push는 파일 보관·앱 배포일 뿐 DB를 바꾸지 않음 (이번 세션 내내 가장 큰 혼동 포인트 — 매 batch마다 mover+push는 했는데 SQL Editor 실행을 빠뜨려서 데이터가 안 바뀜. 다음 세션에선 mover 직후 SQL Editor Run을 한 묶음으로).

---

## 핵심 방침 (확정)
**Deep Tech / Advanced Materials / Life Science 이 세 투자자 그룹이 이 CRM의 핵심.** 마린바이오/필러 사업의 투자자 발굴 대상. 이 세 섹터는 다른 섹터보다 **데이터 정확도(태깅 누락 0, 중복 0)를 우선 관리**. 대형이라도 섹터 부적합(예: Sequoia=핀테크/소비자/SaaS 중심)이면 타깃 섹터로 태깅하지 않음 — 필터 정확도가 펀드 크기보다 중요.

## 핵심 3섹터 현황 (2026-06-12 종료 시점)
- **life_science: 5 → 57** (세션 시작 대비 11배)
- **advanced_materials: 38 → 49**
- **deep_tech: 37 → 46**
- Investors 총: 296 → 약 346 (신규 +57, 중복 -7)

---

## 오늘 적용한 마이그레이션 7개 (DB + repo 모두 적용 확인됨)
`supabase/migrations/`:
1. **20260612010000** — Investors 파이프라인 Won/Lost 스테이지 삭제 (9→7). 딜 outcome은 `deals.status`+`won_lost_reason`+`actual_close_date`로 관리. 칸반은 `stages.map`으로 DB 스테이지를 sort_order대로 렌더 → 안전 확인됨. won/lost 관리 UI(`settings/pipelines/*`)는 타 파이프라인용으로 유지.
2. **20260612020000** — 7개 스테이지 체크리스트 32 + 태스크 템플릿 21 시드. (org_id 명시 필수: default `app.current_organization_id()`가 SQL Editor에선 NULL.)
3. **20260612030000** — 기존 TX 투자자 태깅 보정 (파티는 충분, 태깅 누락이 문제였음). 섹터 10건 + 스테이지 7건.
4. **20260612040000** — 신규 TX: Gigafund(deep_tech), Bios Partners(life_science). source=`investor_enrich_2026Q3`.
5. **20260612050000** — 전국 LS 신규 34개 (batch2). source=`investor_enrich_2026Q3`. ← batch1과 같은 태그라 분리 롤백 시 party_name 예외 필요.
6. **20260612060000** — batch3: LS 대형 크로스오버 12 (EcoR1, Perceptive, RTW, Deep Track, venBio, Vida, Baker Brothers, Redmile, MPM BioImpact, Cormorant, Avoro, Logos) + AM/DT 기업형 CVC 9 (BASF VC, Saint-Gobain NOVA, GM Ventures, Diamond Edge, Toyota Ventures, In-Q-Tel, Prime Movers Lab, Mitsui, Sumitomo/Presidio). source=`investor_enrich_2026Q3_b3` (독립 롤백 가능).
7. **20260612070000** — 중복 대형펀드 정리. 레거시 ingest가 만든 이중행 6쌍을 태그 합집합 병합 후 중복 soft-delete. KEEP: Data Collective Venture Capital / Founders Fund LLC / General Catalyst Partners LLC / Khosla Ventures LLC / Lux Capital Management LLC(deal 보유) / New Enterprise Associates (NEA). (DCVC Bio는 별개 회사 — 유지.)

---

## source 태그 정리 (관리 메모)
- `investor_enrich_2026Q3` = batch1(Gigafund, Bios Partners) + batch2(전국 LS 34). 분리 롤백 시 `and party_name not in ('Gigafund','Bios Partners')`.
- `investor_enrich_2026Q3_b3` = batch3 (21개). 독립 롤백 가능.
- 다음 batch부터는 `_b4`, `_b5` 식으로 분리 권장.
- batch2 광범위 태깅 주의 3개(Mubadala, Decheng, Lightspeed): 순수 LS 아님. 거슬리면 sector_focus에서 제거.

---

## 남은 로드맵
### Investors (3섹터 중심)
- **AM/DT는 이미 포화에 가까움** (각 46~49). 추가 시 대부분 기업형 CVC거나 해외 펌 → 한계효용 낮음. 신규보다 **기존 태깅 정확도 점검**이 더 가치 있음.
- LS는 57로 가장 충실해짐. 추가 여지는 있으나 우선순위 낮음.
- **남은 중복 가능성 점검**: 다른 섹터에도 LLC / 약어 이중행이 있을 수 있음.
- 지역 컬럼 정규화: region에 California(전체명)와 CA(약어) 혼재 — 통일하면 텍사스식 필터가 깔끔.

### Engagement 기능 (2026-06-11 핸드오프에서 이월, 미착수)
- 4단계: 활동 soft-delete 버튼 (`engagements.deleted_at`).
- 5단계: 참석자 미상 → Note 기록 → 나중에 contact 연결 UI.
- 이월: Slack 알림(URM→Slack 단방향), `task_*`→`todo_*` 리네임, `filler_supply_links` 정규화.

---

## 확정 스키마 참고 (INSERT용)
- 투자자 위성 구조: `parties`(party_type_id=1=investor, entity_type_id: 1=company/4=fund) → `investor_profile`(1:1, investor_type_id: 1=vc/2=growth_equity/3=cvc/4=accelerator/5=private_equity/6=family_office/7=angel/11=endowment) → `investor_sector_focus`(M:N) + `investor_stage_focus`(M:N). 둘 다 organization_id NOT NULL.
- sectors: advanced_materials=1, industrial=2, deep_tech=3, climate=4, energy=5, ai=6, software=7, fintech=8, healthcare=9, consumer=10, mobility=11, food_ag=12, defense=13, enterprise=14, crypto=15, life_science=16.
- investment_stages: pre_seed=1, seed=2, series_a=3, series_b=4, series_c=5, series_d=6, early=10, growth=11, late=12, buyout=20, public=21.
- parties 지역: region(주로 2자 코드), city. country_code 별도.
- investor_profile에 sector_focus/geographic_focus text[] 배열도 있음 — 신규 시 정규화 테이블과 함께 채움.
- FK ON DELETE: investor_profile→parties=CASCADE, sector_focus/stage_focus→investor_profile=CASCADE.
- 섹터 필터 URL: `?sector=<code>`.
- Investors 파이프라인 id: `de80525d-5537-4971-ad8f-d2080a8e65b0`. 스테이지 7개(cold_outreach→reply_received→first_meeting→due_diligence→followup_meeting→term_sheet→contract).

## 관례 (유지)
- 추측 INSERT 금지: NOT NULL/enum/FK는 라이브 스키마 확인 후. 진단 쿼리 먼저.
- 실제 VC는 웹 검색 검증 후 사용. 펀드 크기 아닌 섹터 적합성으로 판단.
- 신규 batch는 중복 방지 쿼리(기존 섹터 펌 목록) 먼저 실행.
- **SQL Editor Run = DB 반영** (mover/commit/push는 별개). 적용 후 Verify 쿼리/UI로 확인.
- PowerShell 5.x: 콘솔 ASCII만, .ps1은 Unblock-File, Korean은 UTF-8 BOM .md에만.
