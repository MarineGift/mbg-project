# HANDOFF — 2026-06-12 (Email Pipeline Recovery + Self Party)

## 이번 세션 요약

"whitelist 등록 메일이 URM에 안 들어온다"는 신고에서 시작해, 두 개의 독립 버그와 데이터 정합성 문제를 해결했다.

### 1. 템플릿 토큰 치환 회귀 (배포 완료)
- 증상: Send Email > Template 탭에서 `{{contact_first_name}}` 등이 빈칸으로 치환됨 ("Dear ,").
- 원인: (a) 2026-06-10 재시딩된 템플릿이 snake_case 토큰을 쓰는데 렌더러는 dot-notation만 지원, (b) contact picker가 비어 있는 상태에서 치환 실행.
- 수정: `merge-fields.ts`에 `keepUnknown` 옵션, `compose-email-dialog.tsx`에 rawTemplate 보존 + contact 선택 시 재치환, `send-outbound.ts` `renderWithContext`에 snake_case/sender 토큰 별칭 추가.
- 커밋: `244a8eb` (origin/marinebiogroup, 배포됨).

### 2. 메일 인입 멈춤의 근본 원인 — `communications.module` 42703 (배포 완료)
- 증상: omya.com / mineralstech.com 메일이 인입 안 됨, 일부 메일 56회/39회 중복.
- 원인: `processor.ts:200`의 `fetchCommunicationContext`가 **삭제된 `communications.module` 컬럼**을 SELECT → `processInbound` throw → last_uid 커밋 실패 → 폴링마다 재인입 루프. persistInbound는 성공해 행은 저장됐으나 engagement 연결 단계에서 크래시.
- `module` 컬럼은 party_type 정규화 때 제거됨(이름 일관성). `party_type_id`로 대체됐으나 워커 코드 미반영.
- 수정: SELECT에서 `module` 제거, 매핑 `module: undefined` (auto-send-gate는 partyType undefined면 module 체크 스킵하므로 안전). party_type_id 기반 정확 복원은 TODO 주석.
- 커밋: `9a78ef6` (배포됨).

### 3. 데이터 정리 (SQL, 완료)
- 중복 inbound 125건 soft-delete (cleanup_dup_inbound).
- 발신자 7명 contact 생성 + 미연결 메일 backfill (backfill_inbound_parties_2026Q2_v2):
  - jaehoon.cho@omya.com → Omya (Korea) `fac3df6e-...`
  - edgar.habich@omya.com → Omya (HQ) `c587d9f3-...`
  - sharad.mathur / ken.mueller / raina.wickkiser @mineralstech.com → Specialty Minerals (HQ) `9f161ff5-...`
  - ybseo@cnu.ac.kr, ybseo@arame.kr → MarineBio Group (self), CTO, party `7ace7025-705a-44e3-8209-ae010846d6e6`
  - 모두 `source='inbound_backfill_2026Q2'`, contact_type: mill=partner(2), ybseo=executive(5).
- 더미 test contact 43건 soft-delete (`source='dummy_seed_2026Q2'`, test+omya-XX@example.com). 연결된 communications 1건 unlink 완료.
- 주의: backfill SQL은 BEGIN/COMMIT 트랜잭션으로 실행하면 Supabase SQL Editor에서 세션 종료 시 자동 롤백됨. **BEGIN/COMMIT 없이 각 문 auto-commit으로 실행해야 영구 반영.**

### 4. self party_type + 디렉토리 + Contacts 탭 버그 (커밋 대기)
- `app.party_types`에 `self`(id=10) 추가, MarineBio Group 자사 party 생성 (entity_type=company).
- 사업 관점 확정: 모든 paper mill / filler supplier는 경쟁사가 아니라 **기술 제공 partner**.
- 발견: `src/types/party-type.ts`가 DB(9종)보다 뒤처져 7종만 하드코딩. consultant(8)/crowdfunding_platform(9)도 누락.
- 수정 (커밋 대기):
  - `party-type.ts`: self/consultant/crowdfunding_platform 3종 추가 (union, ID 양방향 매핑, display).
  - `sidebar.tsx`: DIRECTORY에 "MarineBio Group" (`/self/parties`), 색상 teal.
  - `party-detail.ts`: contacts 쿼리에 `.is('deleted_at', null)` 추가 (soft-deleted contact가 Contacts 탭에 보이던 버그 + count 정정).
  - tsc 연쇄 에러 4파일 수정: `[partyType]/parties/page.tsx` (MODULE_LABELS), `party-type-badge.tsx` (PARTY_TYPE_STYLES), `inline-party-create-dialog.tsx` (z.enum), `party-form.tsx` (z.enum) — 모두 3종 추가.

## 커밋 대기 파일 (7개)

mover 2개로 분리 배포:
1. `move_self-party-route_2026-06-12.ps1` → party-type.ts, sidebar.tsx, party-detail.ts
2. `move_party-type-fix_2026-06-12.ps1` → dir-page.tsx, party-type-badge.tsx, inline-party-create-dialog.tsx, party-form.tsx

```
npx tsc --noEmit   # expect 0 errors after both movers
git add src/types/party-type.ts src/components/layout/sidebar.tsx src/lib/queries/party-detail.ts src/app/"(app)"/"[partyType]"/parties/page.tsx src/components/common/party-type-badge.tsx src/components/parties/inline-party-create-dialog.tsx src/components/parties/party-form.tsx
git commit -m "feat(parties): add self/consultant/crowdfunding party types, MarineBio Group directory, fix deleted-contact leak"
git push origin marinebiogroup
```

## 확인할 잠재 이슈 (배포 후)
- **i18n 누락 가능성**: `party-type-badge.tsx`의 `t(partyType)`가 `useTranslations('partyTypes')`를 쓴다. self/consultant/crowdfunding_platform 키가 messages 파일에 없으면 배지 라벨이 키 문자열로 표시됨(빌드는 통과). messages 파일 경로 확인 후 3종 라벨 추가 필요. (messages/en.json 등 표준 경로엔 없었음 — 실제 위치 확인 요)

## 다음 세션 작업 (우선순위 순)

### A. whitelist 등록 UI 개선 (원래 요청, 미착수)
- 목적: 도메인:party = **1:N**. 수신 메일을 발신자별로 올바른 party에 자동 연결.
- 설계 결론: whitelist는 수신 게이트로 유지. party 자동 연결은 matcher가 보는 `contacts.email` 1순위 매칭에 의존하므로, **whitelist 등록 화면(기존 Settings > Email Whitelist 개선)에서 "Email Address" 등록 시 party 선택 + contact 생성**을 함께 하는 흐름이 1:N과 기존 matcher에 가장 적합. (도메인 단위 fallback이 필요하면 whitelist row에 optional default_party_id 추가 검토.)
- 관련 파일:
  - UI: `src/components/settings/email-whitelist-client.tsx` (AddForm 확장)
  - action: `src/lib/actions/email-whitelist.ts` (현재 RPC 기반: add_email_whitelist 등. party+contact 결합 액션 신규 필요)
  - matcher: `src/lib/email/header-parser.ts` `matchSenderToContactAndParty` (현 로직: contacts.email 정확일치 → 도메인 일치 contact의 party_id. generic 도메인 제외. 변경 불필요할 수 있음)
- party 검색 server action 신규 + party type=self 포함 고려.

### B. party_type DB 동적 로딩 리팩터링 (이번 세션에서 분리)
- 현재 `src/types/party-type.ts`가 union/ID매핑/display를 하드코딩. party_type 추가 시마다 TS 수정 + 연쇄 에러(이번에 7개) 발생.
- 목표: `app.party_types`를 서버에서 로드해 context/캐시로 내려주고 하드코딩 제거.
- 영향 범위 큼 (PARTY_TYPE_* import 40+ 파일). union을 string으로 바꾸면 타입 안전성 손실 → 신중한 설계 필요. tsc 게이트 통과시키며 단계적 진행.
- 임시로 z.enum 2곳(inline-party-create-dialog, party-form)과 Record 맵 2곳(dir-page, badge)도 하드코딩 — 동적화 시 함께 정리.

## 참고 ID
- org: b25de8f2-1020-482f-9012-183f63883169
- Omya (HQ): c587d9f3-0fee-435d-a2b7-7beb5b442e07
- Omya (Korea): fac3df6e-875a-4cc3-97d4-fbb0ca10dd7f
- Specialty Minerals (HQ): 9f161ff5-c369-4ca2-8a22-46a1ec40fd0d
- MarineBio Group (self): 7ace7025-705a-44e3-8209-ae010846d6e6
- party_types: investor1 paper_mill2 filler_supplier3 buyer4 customer5 partner6 government_grant7 consultant8 crowdfunding_platform9 self10
- contact_types: employee1 partner2 consultant3 advisor4 executive5 investor_partner6
- entity_types: company1 organization2 individual3 fund4 government5
