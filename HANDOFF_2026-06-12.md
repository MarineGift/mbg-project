\ufeff# URM 세션 Handoff — 2026-06-12

Repo: `MarineGift/mbg-project` · branch `marinebiogroup` · local `C:\dev\mbg-project`
Live: `urm.marinebiogroup.com` (Railway, push = auto-deploy)
Supabase: project `ogenmrgxwhpbfepeldqx`, org_id `b25de8f2-1020-482f-9012-183f63883169`, schema `app`
워크플로: 파일 → Downloads → ASCII mover .ps1 → `npx tsc --noEmit`(0) → git add/commit/push. **SQL은 반드시 Supabase SQL Editor에서 별도 실행** (git commit/push는 DB를 바꾸지 않음 — 이번 세션 내내 혼동 포인트였음).

---

## 오늘 완료한 작업 (DB + repo 양쪽 모두 적용 확인됨)

오늘의 목표: 투자자 파이프라인 정리 + 스테이지별 체크리스트/태스크 템플릿 + Investors 데이터 보강(특히 취약했던 Deep Tech / Advanced Materials / Life Science).

마이그레이션 5개 (`supabase/migrations/`), 전부 SQL Editor 실행 완료 + push 완료:

### 1) 20260612010000_investors_remove_won_lost_stages.sql
- Investors 파이프라인(`de80525d-...`)에서 **Won/Lost 스테이지 삭제** → 9개 → **7개** (cold_outreach, reply_received, first_meeting, due_diligence, followup_meeting, term_sheet, contract).
- 근거: 딜 outcome은 `deals.status` + `won_lost_reason` + `actual_close_date`로 이미 관리됨. 터미널 상태는 칸반 컬럼이 불필요.
- 안전장치: ① Won/Lost에 딜 있으면 abort(실제 0건이라 통과), ② `pg_constraint`로 stages 참조 테이블 동적 탐색 후 정리(컬럼명 추측 없이).
- 코드 확인 완료: 칸반(`kanban-client.tsx:232`)은 `stages.map`으로 DB 스테이지를 sort_order대로 단순 렌더 → 컬럼 사라질 뿐 안전. won/lost 관리 UI(`settings/pipelines/*`)는 paper_mill 등 타 파이프라인용으로 그대로 유지.

### 2) 20260612020000_investor_stage_templates_seed.sql
- 남은 7개 스테이지에 **체크리스트 32항목 + 태스크 템플릿 21개** 시드 (replace 방식: 투자자 파이프라인 기존 템플릿 삭제 후 재시드).
- 주의 발견: `stage_checklist_templates`/`stage_task_templates`의 organization_id default가 `app.current_organization_id()` → SQL Editor에선 NULL → **org_id 명시 INSERT 필수** (반영됨).

### 3) 20260612030000_tx_investor_target_sector_tagging.sql
- 기존 텍사스 투자자(77개)의 **태깅 누락 보정**. 파티 부족이 아니라 sector/stage focus 미태깅이 진짜 원인이었음.
- 섹터 추가 10건: life_science → Santé, TMCi, Health Wildcatters, Green Park & Golf, Vesalius, First Bight / deep_tech → First Bight, Trust Ventures / advanced_materials → Scout, Chevron Technology Ventures.
- 스테이지 추가 7건: Santé(seed, series_a), S3(series_a, series_b), Scout(series_a, series_b), KdT(series_a).

### 4) 20260612040000_tx_investor_inserts_batch1.sql
- 신규 TX 펌 2개 (검증됨): **Gigafund**(Austin, deep_tech/energy/mobility/industrial, seed~growth), **Bios Partners**(Fort Worth, life_science/healthcare, seed~series_b).

### 5) 20260612050000_us_lifescience_inserts_batch2.sql
- **전국 라이프사이언스 신규 34개** (Boston/SF/San Diego/NY/Seattle/기타 hub). 전부 기존 목록과 중복 안 됨(확인 후 작성).
- 전원 series_a+series_b 기본, 시드 활발 펌은 seed 추가, 성장단계 펌은 growth 추가.

### 적용 결과 (UI 확인됨)
- Life Science: **5 → 45** (9배). Healthcare: → 104. Deep Tech: 37 → 40. Advanced Materials: 38 → 40.
- Investors 총: 296 → **331**. Series A: → 160, Series B: → 40.
- 투자자 칸반: 7컬럼 (Won/Lost 사라짐).

---

## 알아둘 것 / 정리하면 좋은 것
- **source 태그 공유**: batch1(Gigafund, Bios Partners)과 batch2가 둘 다 `source='investor_enrich_2026Q3'`. batch2만 롤백하려면 `and party_name not in ('Gigafund','Bios Partners')` 필요. 다음에 batch별 구분이 필요하면 태그를 `..._2026Q3_b1` / `_b2` 식으로 나누는 것 고려.
- **batch2 광범위 태깅 주의 3개**: Mubadala Capital Ventures, Decheng Capital, Lightspeed Venture Partners — 순수 LS 전문이 아닌 멀티섹터/제너럴리스트인데 life_science+healthcare로 태깅됨. 거슬리면 sector_focus에서 제거 가능.
- 전체 enrich 롤백: `delete from app.parties where source='investor_enrich_2026Q3';` (investor_profile/sector_focus/stage_focus까지 FK CASCADE로 자동 정리 — ON DELETE 동작 확인 완료: profile→parties=c, focus→profile=c).

---

## 남은 로드맵 (Investors 보강)

### batch3 후보 (다음 우선순위)
1. **Advanced Materials / Deep Tech 전국 확장** — life_science는 45로 보강 완료. 이제 advanced_materials(40)·deep_tech(40)를 같은 방식으로 전국 Series A+ 펌으로 확장. 후보 리서치부터: 첨단소재·하드테크 전문 VC (예: Eclipse, Lux는 이미 있음 → 중복 체크 먼저).
2. **중복 방지 쿼리 먼저**: batch마다 `select distinct party_name from ... where sector in (타깃)` 으로 기존 목록 뽑고 시작 (이번 세션에서 효과 확인됨).
3. **미국 외 확장**(낮은 우선순위): 유럽/아시아 딥테크·소재 VC. country_code 다양화.

### Engagement 기능 로드맵 (2026-06-11 핸드오프에서 이월, 미착수)
- **4단계**: 활동 soft-delete 버튼 (`engagements.deleted_at`). "중간 Email 삭제" 요청과 연결.
- **5단계**: "참석자 미상 → Note로 먼저 기록 → 나중에 미연결 리스트에서 contact 연결" 흐름 UI.
- 그 외 이월: Slack 알림(URM→Slack 단방향), `task_*`→`todo_*` 리네임, `filler_supply_links` 정규화.

---

## 확정된 스키마 참고 (다음 세션 INSERT용)
- 투자자 = 위성 구조: `parties`(party_type_id=1=investor, entity_type_id=1=company) → `investor_profile`(1:1, investor_type_id: 1=vc/2=growth_equity/3=cvc/4=accelerator/5=private_equity/6=family_office/7=angel/11=endowment) → `investor_sector_focus`(M:N) + `investor_stage_focus`(M:N).
- sectors: advanced_materials=1, industrial=2, deep_tech=3, climate=4, energy=5, ai=6, software=7, fintech=8, healthcare=9, life_science=16, consumer=10, mobility=11, food_ag=12, defense=13, enterprise=14, crypto=15.
- investment_stages: pre_seed=1, seed=2, series_a=3, series_b=4, series_c=5, series_d=6, early=10, growth=11, late=12, buyout=20, public=21.
- parties 지역 컬럼: `region`(주로 'TX','CA' 등 2자 코드), `city`. country_code 별도.
- investor_profile에 `sector_focus`/`geographic_focus` text[] 배열도 있음 — 신규 INSERT 시 정규화 테이블과 함께 채워줌.
- 섹터 필터 URL: `?sector=<code>` (예: `?sector=life_science`).

## 주의/관례 (유지)
- 추측 INSERT 금지: NOT NULL/enum/FK는 라이브 스키마 확인 후 진행.
- 실제 VC 데이터는 웹 검색으로 최신 확인 후 사용.
- **SQL은 SQL Editor에서 실행해야 DB 반영** (commit/push는 파일 보관·앱 배포일 뿐). 적용 후 UI 또는 Verify 쿼리로 확인.
- PowerShell 5.x: 콘솔 출력 ASCII만, 다운로드 .ps1은 Unblock-File, Korean은 UTF-8 BOM .md에만.
